import React, { Component } from "react";
import classes from "./Footer.module.css";

export default class Footer extends Component {
  render() {
    return (
      <div>
        <Footer className={classes.Footer}>
          <div>Contacts and Support</div>
        </Footer>
      </div>
    );
  }
}
